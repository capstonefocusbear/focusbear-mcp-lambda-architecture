meta {
  name: /integration/<platform>/user-projects
  type: http
  seq: 12
}

get {
  url: {{base_url}}/integration/monday/user-projects
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
