import { ViewAnnouncementDto } from './view-announcement.dto';

describe('ViewAnnouncementDto', () => {
  it('should be defined', () => {
    expect(new ViewAnnouncementDto()).toBeDefined();
  });
});
