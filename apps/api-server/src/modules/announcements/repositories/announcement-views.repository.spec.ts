import { AnnouncementViewsRepository } from './announcement-views.repository';

describe('AnnouncementViewsRepository', () => {
  it('should be defined', () => {
    expect(new AnnouncementViewsRepository()).toBeDefined();
  });
});
