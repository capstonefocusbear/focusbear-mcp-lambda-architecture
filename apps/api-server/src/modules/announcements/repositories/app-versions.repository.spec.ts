import { AppVersionsRepository } from './app-versions.repository';

describe('AppVersionsRepository', () => {
  it('should be defined', () => {
    expect(new AppVersionsRepository()).toBeDefined();
  });
});
